<!doctype html>
<html lang="{{ current_site.locale | default: "en" }}">
  {% include "global_head" %}

  {{- '<!-- RMA Brand Fonts -->' }}
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600;700;800;900&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" />
  <link rel="stylesheet" href="{{ 'rma-brand.css' | asset_url }}" />

  {% include "global_background" %}
  <body id="encore-theme" class="background-{{ settings.background_color | color_scheme_class }} sage-excluded">
    {% include "alert_messages" %}
    {% section "header" %}
    <main>
      {{ content_for_layout }}
    </main>
    {% section "footer" %}
    {% section "exit_pop" %}
    {% section "two_step" %}
    {% include "global_scripts" %}
  </body>
</html>
